{% extends 'base.html.twig' %}

{% block title %}{% trans %}Admin Info{% endtrans %}{% endblock %}

{% block body %}
<h2>{% trans %}Admin Info{% endtrans %}</h2>

<h4>Server</h4>
<table class='table'>
  <tr><th>DOCUMENT_ROOT</th><td> {{ server.DOCUMENT_ROOT | default('n/a') }}</tr>
  <tr><th>SERVER_SIGNATURE</th><td> {{ server.SERVER_SIGNATURE | default('n/a') }}</tr>
  <tr><th>SERVER_SOFTWARE</th><td> {{ server.SERVER_SOFTWARE | default('n/a') }}</tr>
  <tr><th>SERVER_ADMIN</th><td> {{ server.SERVER_ADMIN | default('n/a') }}</tr>
  <tr><th>PHP OS</th><td>{{ constant('PHP_OS') | default('n/a') }}</tr>
  <tr><th>PHP version</th><td>{{ constant('PHP_VERSION') | default('n/a') }}</tr>
  <tr><th>PHP binary</th><td>{{ constant('PHP_BINARY') | default('n/a') }}</tr>
</table>

<h4>Application</h4>
<table class='table'>
  <tr><th>APP_ENV</th><td> {{ server.APP_ENV | default('n/a') }}</tr>
  <tr><th>SYMFONY_DOTENV_VARS</th><td> {{ server.SYMFONY_DOTENV_VARS | default('n/a') }}</tr>
  <tr><th>APP_LOG_DIR (Env)</th><td> {{ server.APP_LOG_DIR | default('') }}</tr>
  <tr><th>APP_CACHE_DIR (Env)</th><td> {{ server.APP_CACHE_DIR | default('') }}</tr>
  <tr><th>Cache dir (real)</th><td> {{ cacheDir | default('n/a') }}</tr>
  <tr><th>APP_DEBUG</th><td> {{ server.APP_DEBUG | default('n/a') }}</tr>
  <tr><th>Symfony version</th><td> {{ symfonyversion | default('n/a') }}</tr>
</table>

<h4>Client</h4>
<table class='table'>
  <tr><th>IP</th><td> {{ netInfo.ip | default('n/a') }}</tr>
  <tr><th>Country</th><td> {{ netInfo.country | default('n/a') }}</tr>
  <tr><th>City</th><td> {{ netInfo.city | default('n/a') }}</tr>
  <tr><th>User agent</th><td> {{ netInfo.ua | default('n/a') }}</tr>
  <tr><th>Referer</th><td> {{ netInfo.referer | default('n/a') }}</tr>
</table>
{% endblock %}
